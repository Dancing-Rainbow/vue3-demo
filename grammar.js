// 变量声明
var num = 1;
var str = 'hello';
// 函数参数类型与返回值类型
function sum(a, b) {
    return a + b;
}
// 复合元素类型
var arr = [1, 2, 3];
var point = { x: 10, y: 20 };
var product = function (a, b) { return a * b; };
